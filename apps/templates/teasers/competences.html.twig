<div class="wrapper">
    <h3 class="title">
        {{ item.title }}
    </h3>
    <div class="list-competences">
        {% for competence in item.competences %}
            <span class="icon-{{ competence.icon }}">
                {{ competence.title }}
            </span>
        {% endfor %}
    </div>
</div>