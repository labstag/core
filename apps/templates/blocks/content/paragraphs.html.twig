<div id="{{ block_id(block) }}" class="{{ block_class(block) }}"{{ block_contextmenu(block) }}>
    {% for tab in paragraphs %}
        {{ debug_begin(tab) }}
        {{ paragraph_show(tab) }}
        {{ debug_end(tab) }}
    {% endfor %}
</div>