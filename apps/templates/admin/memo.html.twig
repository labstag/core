<div class="card mb-3">
    <div class="card-header">
        {{ memo.title }}
    </div>
    <div class="card-body">
        {% for tab in paragraphs[memo.id] %}
            {{ debug_begin(tab) }}
            {{ paragraph_show(tab) }}
            {{ debug_end(tab) }}
        {% endfor %}
    </div>
</div>