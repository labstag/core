<section id="{{ paragraph_id(paragraph) }}" class="{{ paragraph_class(paragraph) }}"{{ paragraph_contextmenu(paragraph) }}>
    <div class="wrapper">
        {% if paragraph.title|length != 0 %}
            <h3 class="title">
                {{ paragraph.title }}
            </h3>
        {% endif %}
        <ul>
            {% for row in chapters %}
                <li class="teaser-chapter">
                    {% include  "teasers/chapter.html.twig"  with {'chapter': row} %}
                </li>
            {% endfor %}
        </ul>
    </div>
</section>