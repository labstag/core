<section id="{{ paragraph_id(paragraph) }}" class="{{ paragraph_class(paragraph) }}">
    <div class="background" style="background-image: url('{{ site_asset(post, 'img') }}')"></div>
    <div class="title">
        {{ post.title }}
    </div>
    <div class="date">
        {{ post.createdat|date('d/m/Y') }}
    </div>
</section>