<section id="{{ paragraph_id(paragraph) }}">
    <h2>{{ paragraph.title }}</h2>
    <h3>{{ edito.title }}</h3>
    {% for tab in paragraphs %}
        {{ debug_begin(tab) }}
        {{ paragraph_show(tab) }}
        {{ debug_end(tab) }}
    {% endfor %}
</section>