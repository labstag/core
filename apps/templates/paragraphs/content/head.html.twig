<section id="{{ paragraph_id(paragraph) }}" class="{{ paragraph_class(paragraph) }}"{{ paragraph_contextmenu(paragraph) }}>
    <div class="background" style="--image-url: url('{{ site_asset(data.entity, 'img') }}')"></div>
	<div class="wrapper">
		<h1 class="title">
			{{ data.entity.title }}
		</h1>
    </div>
</section>