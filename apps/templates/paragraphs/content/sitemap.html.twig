<section id="{{ paragraph_id(paragraph) }}" class="{{ paragraph_class(paragraph) }}">
    <div class="wrapper">{% include  "paragraphs/scripts/sitemap.html.twig"  with {'key': '/', 'data': sitemap} %}</div>
</section>