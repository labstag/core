<section id="{{ paragraph_id(paragraph) }}" class="{{ paragraph_class(paragraph) }}">
    <h2>{{ paragraph.title }}</h2>
    <div>
        <div>
            <ul>
                {% for row in news %}
                    <li>
                        {% include  "teasers/post.html.twig"  with {'post': row} %}
                    </li>
                {% endfor %}
            </ul>
        </div>
        <div><a href="{{ path('front', {'slug': listing.slug}) }}" class="btn btn-primary">Voir toute les actualités</a></div>
    </div>
</section>