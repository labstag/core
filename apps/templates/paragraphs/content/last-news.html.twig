<section id="{{ paragraph_id(paragraph) }}" class="{{ paragraph_class(paragraph) }}">
    <div class="wrapper">
        <div class="title">
            <h2>{{ paragraph.title }}</h2>
        </div>
        <div class="link">
            <a href="{{ path('front', {'slug': listing.slug}) }}" class="btn">Voir toute les actualités</a>
        </div>
        <div class="list">
            <ul>
                {% for row in news %}
                    <li class="post-teaser">
                        {% include  "teasers/post.html.twig"  with {'post': row} %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</section>