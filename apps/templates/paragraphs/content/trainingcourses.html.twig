{% use "elements.html.twig" %}
<section{% with {attr: paragraph_contextmenu(paragraph)} %}{{ block('attributes') }}{% endwith %}>
    <div class="wrapper">
        <h2>{{ paragraph.title}}</h2>
        <ul class="listetrainingcourses">
            {% for item in paragraph.trainings %}
                <li class="teaser-training">
                    {% include "teasers/training.html.twig"  with {'item': item} %}
                </li>
            {% endfor %}
        </ul>
    </div>
</section>