# Read the documentation at https://github.com/thephpleague/flysystem-bundle/blob/master/docs/1-getting-started.md
flysystem:
  storages:
    private.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/private'
    public.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads'
    avatar.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/avatar'
    edito.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/edito'
    page.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/page'
    chapter.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/chapter'
    memo.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/memo'
    history.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/history'
    post.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/post'
