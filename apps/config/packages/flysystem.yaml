# Read the documentation at https://github.com/thephpleague/flysystem-bundle/blob/master/docs/1-getting-started.md
flysystem:
  storages:
    private.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/private'
    public.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads'
    assets.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/assets'
    person.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/person'
    game.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/game'
    company.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/company'
    platform.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/platform'
    media.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/media'
    configuration.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/configuration'
    avatar.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/avatar'
    chapter.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/chapter'
    movie.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/movie'
    serie.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/serie'
    episode.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/episode'
    saga.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/saga'
    season.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/season'
    edito.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/edito'
    story.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/story'
    memo.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/memo'
    page.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/page'
    paragraph.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/paragraph'
    post.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/post'
    star.storage:
      adapter: 'local'
      options:
        directory: '%kernel.project_dir%/public/uploads/star'
