# Configuration d'environnement pour les tests
APP_ENV=test
APP_SECRET=4912efaa7a9bc10abd32a1f3b1be35f9

# Base de données de test (SQLite fichier pour partager le schéma entre kernels)
DATABASE_URL="sqlite:///%kernel.project_dir%/var/behat.db"

# Messenger en mode synchrone pour les tests
MESSENGER_TRANSPORT_DSN=sync://

# CORS pour les tests
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'

# Proxies de confiance
TRUSTED_PROXIES=127.0.0.1,10.0.0.0/8

# Clés API de test (valeurs factices)
OMDB_API_KEY=test_omdb_key
TMDB_API_KEY=test_tmdb_key

# Désactiver les notifications externes en test
MASTODON_DSN=
DISCORD_DSN=
BLUESKY_DSN=
